---
 - name: "comand line args"
   hosts: all
   gather_facts: false
   vars:
     x: 5
     y: 6
   tasks:
     - name: "work with debug module"
       debug:
          msg: 
           - "The x value is: {{x}}"
           - "The y value is: {{y}}"
     

---
 - name: This is a simple play to understand debug module 
   hosts: all
   tasks:
   - name: "Print a Simple msg"
     debug:
       msg:
        - "welcome to play book"
        - "Hi demo sede"
        - "3rd msg: {{message}}"
